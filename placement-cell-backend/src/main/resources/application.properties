spring.application.name=Placement-Cell

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/trainingplacementcell}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# JWT Configuration
# NOTE: In production, move this secret to an environment variable
# (e.g. export JWT_SECRET=... and use ${JWT_SECRET} here) instead of committing it to git.
jwt.secret=${JWT_SECRET:mvixRnuERdKIsrQBv+v9mvaT3s2CBbzI4vdc4PELUKG+EhSyopqB/Zmquf1ZTcqqdRxNzSD+fLHbyorMM+edNQ==}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
